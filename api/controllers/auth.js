function signup(req, res) {
    signupAsync(req, res);
}

function signupAsync(req, res) {
    // TODO
}

function login(req, res) {
    loginAsync(req, res);
}

function loginAsync(req, res) {
    // TODO   
}

module.exports = {
    signup,
    login
}